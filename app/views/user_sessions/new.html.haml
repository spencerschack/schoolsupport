= semantic_form_for @user_session, url: login_path,
	html: { class: 'scroller' } do |f|
	= f.semantic_errors
	= f.inputs do
		= f.input :email
		= f.input :password
	= f.actions do
		= f.action :submit, label: 'Login'