= semantic_form_for resource_with_parents(resource) do |f|
	= f.semantic_errors
	= f.inputs do
		- fields(:form).each do |field|
			= f.input *field
			= debug field
	= f.actions do
		= f.action :submit