<%= semantic_form_for @student_note, url: student_new_student_note_path(@student.id) do |f| %>
  <%= f.inputs do %>
    <%= f.input :notes %>
  <% end %>
  <%= f.actions do %>
    <%= f.action :submit, label: 'Add Notes' %>
  <% end %>
<% end %>