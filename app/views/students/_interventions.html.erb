<div class="interventions">
  <h2>Interventions</h2>
  <%= form_for @student, class: 'special_handler' do |f| %>
    <div class="table">
      <div>
        <span>Name</span>
        <span>Start</span>
        <span>Stop</span>
        <span>Notes</span>
      </div>
      <% sort_interventions(@student.interventions).each do |intervention| %>
        <%= f.fields_for :interventions, intervention do |i| %>
          <a class="intervention">
            <span>
              <%= i.text_area :name %>
              <em class="holder"><%= format_holder_text(intervention.name) %></em>
            </span>
            <span>
              <%= i.text_area :start %>
              <em class="holder"><%= format_holder_text(intervention.start) %></em>
            </span>
            <span>
              <%= i.text_area :stop %>
              <em class="holder"><%= format_holder_text(intervention.stop) %></em>
            </span>
            <span>
              <%= i.text_area :notes %>
              <em class="holder"><%= format_holder_text(intervention.notes) %></em>
            </span>
          </a>
        <% end %>
      <% end %>
    </div>
  <% end %>
</div>